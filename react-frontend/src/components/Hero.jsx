// Hero.jsx
import "../App.css"
export default function Hero() {
    return (
        <section className="hero">
            <h1>Discover Articles</h1>
            <p>
                Explore articles fetched from a Laravel backend with version history
                and updates.
            </p>
        </section>
    );
}
